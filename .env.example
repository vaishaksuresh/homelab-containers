# Homelab Container Environment Variables
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git (already in .gitignore)

# ==========================================
# WUD (What's Up Docker) Configuration
# ==========================================

# WUD Web UI Authentication
# Generate hash: htpasswd -nb admin yourpassword
# Or use: https://hostingcanada.org/htpasswd-generator/
# Take the part after the colon, replace $ with $$
# Example: $apr1$abc123 becomes $$apr1$$abc123
WUD_AUTH_USER=admin
WUD_AUTH_HASH=$$apr1$$REPLACE_THIS_WITH_YOUR_HASH

# ==========================================
# Discord Notifications
# ==========================================

# Discord webhook URL for container update notifications
# Get this from Discord Server Settings > Integrations > Webhooks
# Used by both WUD and Watchtower
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# ==========================================
# Service Passwords
# ==========================================

# Pi-hole web interface password
PIHOLE_PASSWORD=your_pihole_password_here

# Immich database password
IMMICH_DB_PASSWORD=your_immich_db_password_here

# ==========================================
# Optional: Additional Configuration
# ==========================================

# Timezone (used by some containers)
TZ=America/Los_Angeles

# ==========================================
# Notes
# ==========================================

# How to generate WUD password hash:
# 1. Install apache2-utils: sudo apt-get install apache2-utils (or brew install httpd on macOS)
# 2. Run: htpasswd -nb admin yourpassword
# 3. Copy everything after "admin:" including the $
# 4. Replace every $ with $$ (double dollar signs)
# 5. Paste into WUD_AUTH_HASH above
#
# Example:
#   htpasswd output: admin:$apr1$abc123$xyz789
#   Use in .env: $$apr1$$abc123$$xyz789

# How to get Discord webhook:
# 1. Go to your Discord server
# 2. Right-click the channel you want notifications in
# 3. Select "Edit Channel" > "Integrations" > "Webhooks"
# 4. Click "New Webhook" or use existing one
# 5. Copy the webhook URL
# 6. Paste into DISCORD_WEBHOOK_URL above
